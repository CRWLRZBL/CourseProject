# Автосалон LADA

Веб-приложение для управления автосалоном автомобилей LADA. Система включает каталог автомобилей, онлайн-конфигуратор, управление заказами и административную панель.

## 📋 Содержание

- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Предварительные требования](#предварительные-требования)
- [Установка и запуск](#установка-и-запуск)
- [Использование](#использование)
- [API документация](#api-документация)
- [База данных](#база-данных)
- [Разработка](#разработка)
- [Troubleshooting](#troubleshooting)
- [Документация проекта](#документация-проекта)

---

## 🛠 Технологический стек

### Backend
- **.NET 8.0** - Основной фреймворк
- **ASP.NET Core Web API** - REST API
- **Entity Framework Core** - ORM для работы с базой данных
- **SQL Server 2022** - База данных
- **QuestPDF** - Генерация PDF отчетов
- **EPPlus** - Работа с Excel файлами

### Frontend
- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик и dev-сервер
- **React Bootstrap** - UI компоненты
- **React Router** - Маршрутизация
- **Axios** - HTTP клиент
- **Google Material Icons** - Иконки

### Инфраструктура
- **Docker** - Контейнеризация
- **Docker Compose** - Оркестрация контейнеров
- **Nginx** - Веб-сервер для frontend

---

## 📁 Структура проекта

```
Cursa4/
├── backend/                    # Backend приложение
│   ├── CourseProjectAPI/       # Основной проект API
│   │   ├── Controllers/       # API контроллеры
│   │   ├── Services/          # Бизнес-логика
│   │   ├── Models/            # Модели данных
│   │   ├── DTOs/              # Data Transfer Objects
│   │   ├── Data/              # DbContext
│   │   └── database_full_script.sql  # Полный SQL скрипт создания БД
│   └── Dockerfile             # Docker образ для backend
├── frontend/                   # Frontend приложение
│   ├── frontend/              # React приложение
│   │   ├── src/
│   │   │   ├── components/    # React компоненты
│   │   │   ├── pages/         # Страницы
│   │   │   ├── services/      # API сервисы
│   │   │   ├── utils/         # Утилиты
│   │   │   └── context/       # React Context
│   │   └── public/            # Статические файлы
│   ├── Dockerfile             # Docker образ для frontend
│   └── nginx.conf             # Конфигурация Nginx
├── docker-compose.yml          # Конфигурация Docker Compose
└── README.md                   # Этот файл
```

---

## 📦 Предварительные требования

- **Docker Desktop** (версия 20.10 или выше)
- **Docker Compose** (версия 2.0 или выше)
- **Git** (для клонирования репозитория)

Для локальной разработки (без Docker):
- **.NET 8.0 SDK**
- **Node.js** (версия 18 или выше)
- **npm** или **yarn**
- **SQL Server 2022** (или SQL Server Express)

---

## 🚀 Установка и запуск

### Быстрый старт с Docker (рекомендуется)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd Cursa4
   ```

2. **Запустите проект:**

   **На Windows (рекомендуется):**
   ```cmd
   docker-compose-up.bat
   ```
   
   Или вручную:
   ```powershell
   $env:DOCKER_BUILDKIT=0
   $env:COMPOSE_DOCKER_CLI_BUILD=0
   docker-compose up -d --build
   ```

   **На Linux/macOS:**
   ```bash
   docker-compose up -d --build
   ```

3. **Дождитесь запуска всех сервисов:**
   ```bash
   docker-compose ps
   ```
   
   Все три сервиса должны быть в состоянии "Up":
   - `DBproject` (база данных)
   - `backend-api` (backend)
   - `frontend-app` (frontend)

4. **Проверьте работу:**
   - Frontend: http://localhost:3000
   - Backend API: http://localhost:5171
   - Swagger UI: http://localhost:5171/swagger

### Первоначальная настройка базы данных

После первого запуска необходимо выполнить SQL скрипт для создания базы данных:

1. **Выполните единый скрипт создания БД:**

   **Способ 1: Через Docker (рекомендуется)**
   ```bash
   # Скопируйте скрипт в контейнер
   docker cp backend/CourseProjectAPI/database_full_script.sql DBproject:/tmp/
   
   # Выполните скрипт
   docker exec -i DBproject /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P Pass1234 -C -i /tmp/database_full_script.sql
   ```
   
   **Способ 2: Через VS Code SQL Server Extension**
   - Подключитесь к БД (Server: `localhost,1433`, User: `sa`, Password: `Pass1234`)
   - Откройте файл `backend/CourseProjectAPI/database_full_script.sql`
   - Выполните весь скрипт (F5 или кнопка Execute)
   
   **Способ 3: Через sqlcmd на хосте**
   ```bash
   sqlcmd -S localhost,1433 -U sa -P Pass1234 -C -i backend/CourseProjectAPI/database_full_script.sql
   ```

### Локальная разработка (без Docker)

#### Backend

1. **Перейдите в директорию backend:**
   ```bash
   cd backend/CourseProjectAPI
   ```

2. **Восстановите зависимости:**
   ```bash
   dotnet restore
   ```

3. **Настройте строку подключения** в `appsettings.json`:
   ```json
   {
     "ConnectionStrings": {
       "DefaultConnection": "Server=localhost;Database=Autosalon;User Id=sa;Password=YourPassword;TrustServerCertificate=true;"
     }
   }
   ```

4. **Запустите приложение:**
   ```bash
   dotnet run
   ```

   Backend будет доступен на http://localhost:5171

#### Frontend

1. **Перейдите в директорию frontend:**
   ```bash
   cd frontend/frontend
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Создайте файл `.env`** (если нужно изменить API URL):
   ```
   VITE_API_URL=http://localhost:5171/api
   ```

4. **Запустите dev-сервер:**
   ```bash
   npm run dev
   ```

   Frontend будет доступен на http://localhost:5173

---

## 💻 Использование

### Основные функции

1. **Каталог автомобилей**
   - Просмотр всех доступных моделей LADA
   - Фильтрация по типу кузова, цене, году выпуска
   - Просмотр детальной информации об автомобиле

2. **Онлайн-конфигуратор**
   - Выбор модели автомобиля
   - Выбор комплектации
   - Выбор двигателя и трансмиссии
   - Выбор цвета
   - Выбор дополнительных опций
   - Просмотр итоговой стоимости

3. **Оформление заказа**
   - Создание заказа на выбранную конфигурацию
   - Управление заказами в личном кабинете
   - Отслеживание статуса заказа

4. **Административная панель** (только для администраторов)
   - Управление автомобилями
   - Управление заказами
   - Импорт автомобилей из Excel
   - Экспорт отчетов по продажам в PDF
   - Просмотр статистики и аналитики

### Роли пользователей

- **Пользователь (Client)** - может просматривать каталог, использовать конфигуратор и оформлять заказы
- **Менеджер (Manager)** - имеет доступ к управлению заказами
- **Администратор (Admin)** - имеет доступ ко всем функциям, включая административную панель

### Учетные записи по умолчанию

После инициализации базы данных создайте учетные записи через API или напрямую в базе данных.

---

## 📚 API документация

После запуска backend, Swagger UI доступен по адресу: http://localhost:5171/swagger

### Основные endpoints

#### Авторизация
- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему

#### Автомобили
- `GET /api/cars` - Получить список автомобилей
- `GET /api/cars/{id}` - Получить информацию об автомобиле
- `GET /api/cars/models` - Получить список моделей
- `GET /api/cars/models/{modelId}/configurations` - Получить комплектации модели
- `GET /api/cars/colors` - Получить список цветов
- `GET /api/cars/engines` - Получить список двигателей
- `GET /api/cars/transmissions` - Получить список трансмиссий
- `PUT /api/cars/{id}` - Обновить информацию об автомобиле (только для админов)

#### Заказы
- `GET /api/orders` - Получить список заказов
- `GET /api/orders/{id}` - Получить информацию о заказе
- `POST /api/orders` - Создать новый заказ
- `PUT /api/orders/{id}/status` - Обновить статус заказа (только для админов)

#### Отчеты
- `GET /api/reports/sales/export` - Экспорт отчета по продажам в PDF

#### Импорт
- `GET /api/cars/import/template` - Скачать шаблон Excel для импорта
- `POST /api/cars/import/excel` - Импортировать автомобили из Excel (только для админов)

---

## 🗄 База данных

### Структура базы данных

Основные таблицы:
- `Brands` - Бренды автомобилей
- `Models` - Модели автомобилей
- `Configurations` - Комплектации
- `Cars` - Автомобили
- `Colors` - Цвета
- `Engines` - Двигатели
- `Transmissions` - Трансмиссии
- `AdditionalOptions` - Дополнительные опции
- `Orders` - Заказы
- `OrderStatusHistory` - История статусов заказов
- `Users` - Пользователи
- `UserProfiles` - Профили пользователей
- `Roles` - Роли

### Подключение к базе данных

**Через Docker:**
```bash
docker exec -it DBproject /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P Pass1234 -C
```

**Параметры подключения:**
- Сервер: `localhost` (или `db` внутри Docker сети)
- Порт: `1433`
- База данных: `Autosalon`
- Пользователь: `sa`
- Пароль: `Pass1234`

### Выполнение SQL скриптов

**Создание базы данных:**
Выполните единый скрипт `backend/CourseProjectAPI/database_full_script.sql`, который создает всю структуру БД со всеми таблицами, индексами, внешними ключами, триггерами и начальными данными.

**Через Docker:**
```bash
# Скопируйте и выполните скрипт
docker cp backend/CourseProjectAPI/database_full_script.sql DBproject:/tmp/
docker exec -i DBproject /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P Pass1234 -C -i /tmp/database_full_script.sql
```

**Через sqlcmd на хосте:**
```bash
sqlcmd -S localhost,1433 -U sa -P Pass1234 -C -i backend/CourseProjectAPI/database_full_script.sql
```

**Через VS Code SQL Server Extension:**
- Установите расширение "SQL Server (mssql)"
- Создайте новое подключение:
  - Server: `localhost,1433`
  - Database: `Autosalon`
  - Username: `sa`
  - Password: `Pass1234`
  - Encrypt: `false` (важно!)
  - Trust Server Certificate: `true`
- Откройте файл `backend/CourseProjectAPI/database_full_script.sql`
- Нажмите F5 или кнопку "Execute"

### Полезные SQL команды

**Просмотр всех таблиц:**
```sql
SELECT name FROM sys.tables ORDER BY name;
```

**Просмотр данных из таблицы:**
```sql
SELECT * FROM Colors;
SELECT * FROM Engines;
SELECT * FROM Transmissions;
```

**Просмотр структуры таблицы:**
```sql
EXEC sp_help 'Colors';
```

---

## 🔧 Разработка

### Структура компонентов Frontend

```
src/
├── components/
│   ├── common/          # Общие компоненты (Header, Footer, Icon)
│   ├── cars/            # Компоненты для работы с автомобилями
│   │   ├── configurator/ # Компоненты конфигуратора
│   ├── orders/          # Компоненты для работы с заказами
│   └── admin/           # Компоненты административной панели
├── pages/               # Страницы приложения
├── services/            # API сервисы
├── utils/               # Утилиты
└── context/             # React Context (AuthContext)
```

### Добавление новых функций

1. **Backend:**
   - Создайте модель в `Models/`
   - Создайте DTO в `DTOs/`
   - Создайте сервис в `Services/`
   - Создайте контроллер в `Controllers/`
   - Обновите SQL скрипт при необходимости

2. **Frontend:**
   - Создайте компонент в соответствующей директории
   - Добавьте API метод в `services/api/`
   - Добавьте маршрут в `App.jsx` (если нужна новая страница)

### Тестирование

**Backend:**
```bash
cd backend/CourseProjectAPI
dotnet test
```

**Frontend:**
```bash
cd frontend/frontend
npm test
```

---

## 🐛 Troubleshooting

### Проблемы с Docker

**Контейнер не запускается:**
```bash
# Проверьте логи
docker-compose logs <service-name>

# Пересоберите образы
docker-compose build --no-cache

# Удалите старые контейнеры и volumes
docker-compose down -v
```

**Проблемы с BuildKit на Windows:**
```powershell
$env:DOCKER_BUILDKIT=0
$env:COMPOSE_DOCKER_CLI_BUILD=0
docker-compose up -d --build
```

**Docker Desktop "bus error":**
1. Перезапустите Docker Desktop
2. В Диспетчере задач убедитесь, что все процессы Docker завершены
3. Если не помогло, попробуйте сброс к заводским настройкам:
   - Settings → Troubleshoot → Reset to factory defaults
   - ⚠️ Это удалит все контейнеры, образы и volumes!

**Проблемы с WSL2:**
```powershell
# Проверьте версию WSL
wsl --version

# Обновите WSL
wsl --update

# Перезапустите WSL
wsl --shutdown
```

### Проблемы с базой данных

**База данных не подключается:**
- Проверьте, что контейнер `DBproject` запущен: `docker ps`
- Проверьте логи: `docker logs DBproject`
- Убедитесь, что пароль SA правильный в `docker-compose.yml`

**Ошибки при подключении через VS Code:**
- Убедитесь, что "Encrypt" установлен в `false` или `Optional`
- Включите "Trust Server Certificate"
- Сервер должен быть: `localhost,1433` (не `db,1433` при подключении с хоста)

**Ошибки при выполнении SQL скриптов:**
- Убедитесь, что используете правильную кодировку (UTF-8)
- Проверьте, что все зависимости созданы в правильном порядке

**"sqlcmd: command not found":**
Попробуйте другой путь:
- `/opt/mssql-tools18/bin/sqlcmd` (SQL Server 2022)
- `/opt/mssql-tools/bin/sqlcmd` (старые версии)

### Проблемы с Frontend

**Изображения не загружаются:**
- Проверьте, что файлы находятся в `frontend/frontend/public/images/`
- Проверьте конфигурацию Nginx в `frontend/nginx.conf`

**API запросы не работают:**
- Проверьте переменную окружения `VITE_API_URL`
- Проверьте CORS настройки в backend
- Проверьте, что backend запущен и доступен

### Проблемы с Backend

**Ошибки компиляции:**
```bash
# Очистите кэш
dotnet clean
dotnet restore
dotnet build
```

**Ошибки подключения к БД:**
- Проверьте строку подключения в `appsettings.json`
- Убедитесь, что SQL Server запущен и доступен
- Проверьте firewall настройки

---

## 📝 Дополнительная информация

### Переменные окружения

**Backend:**
- `ASPNETCORE_ENVIRONMENT` - Окружение (Development/Production)
- `ConnectionStrings__DefaultConnection` - Строка подключения к БД

**Frontend:**
- `VITE_API_URL` - URL backend API

### Порты

- **3000** - Frontend (Nginx)
- **5171** - Backend API
- **1433** - SQL Server

### Безопасность

⚠️ **Важно:** В production окружении:
- Измените пароль SA для SQL Server
- Используйте HTTPS
- Настройте CORS правильно
- Используйте переменные окружения для секретов
- Не коммитьте `appsettings.json` с реальными паролями

---

## 📄 Документация проекта

### Перечень сокращений и обозначений

**Латинские сокращения:**
- **API** - Application Programming Interface (интерфейс прикладного программирования)
- **ASP.NET Core** - веб-фреймворк от Microsoft
- **CORS** - Cross-Origin Resource Sharing (межсайтовый обмен ресурсами)
- **CRUD** - Create, Read, Update, Delete (создание, чтение, обновление, удаление)
- **DTO** - Data Transfer Object (объект передачи данных)
- **EF Core** - Entity Framework Core (фреймворк для работы с БД)
- **JWT** - JSON Web Token (токен веб-доступа)
- **ORM** - Object-Relational Mapping (объектно-реляционное отображение)
- **REST** - Representational State Transfer (архитектурный стиль веб-служб)
- **SPA** - Single Page Application (одностраничное приложение)

**Кириллические сокращения:**
- **БД** - база данных
- **ИС** - информационная система
- **ПО** - программное обеспечение
- **СУБД** - система управления базами данных

### Изменения API для конфигуратора

#### Новые поля в таблице `Configurations`

Добавлены следующие поля для хранения характеристик двигателя и коробки передач:
- **EnginePower** (INT, NULL) - Мощность двигателя в л.с.
- **EngineCapacity** (DECIMAL(4,2), NULL) - Объем двигателя
- **FuelType** (NVARCHAR(20), NULL) - Тип топлива
- **TransmissionType** (NVARCHAR(20), NULL) - Тип коробки передач

#### API Endpoints для конфигуратора

**Получение цветов:**
```
GET /api/cars/colors
```

**Получение двигателей:**
```
GET /api/cars/engines?modelId={id}
```

**Получение трансмиссий:**
```
GET /api/cars/transmissions?modelId={id}
```

**Получение комплектаций (обновлено):**
```
GET /api/cars/models/{modelId}/configurations
```
Теперь возвращает комплектации с полной информацией о двигателе и КПП.

### Примеры кода

**Frontend - Загрузка списка автомобилей:**
```typescript
const loadCars = async () => {
  try {
    setLoading(true);
    
    const params: Record<string, string> = {};
    
    if (searchValue) {
      params.search = searchValue;
    }
    
    if (statusFilter) {
      params.status = statusFilter;
    }
    
    const response = await api.get<Car[]>('/api/cars', { params });
    setCars(response.data || []);
  } catch (error) {
    console.error('Ошибка загрузки автомобилей:', error);
    toast.error('Не удалось загрузить список автомобилей');
    setCars([]);
  } finally {
    setLoading(false);
  }
};
```

**Backend - Получение списка автомобилей:**
```csharp
[HttpGet]
public async Task<ActionResult<List<CarDto>>> GetCars(
    [FromQuery] string brand = null,
    [FromQuery] string bodyType = null,
    [FromQuery] bool all = false)
{
    try
    {
        if (all)
        {
            var allCars = await _carService.GetAllCarsAsync();
            return Ok(allCars);
        }
        var cars = await _carService.GetAvailableCarsAsync(brand, bodyType);
        return Ok(cars);
    }
    catch (Exception ex)
    {
        return StatusCode(500, new { Error = ex.Message });
    }
}
```

---

## 📄 Лицензия

Этот проект создан в учебных целях.

## 👥 Авторы

Проект разработан в рамках курсового проекта.

---

**Вопросы и предложения:** Создайте issue в репозитории проекта.
