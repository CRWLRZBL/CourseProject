# Автосалон LADA

Веб-приложение для управления автосалоном автомобилей LADA. Система включает каталог автомобилей, онлайн-конфигуратор, управление заказами и административную панель.

## 📋 Содержание

- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Предварительные требования](#предварительные-требования)
- [Установка и запуск](#установка-и-запуск)
- [Использование](#использование)
- [API документация](#api-документация)
- [База данных](#база-данных)
- [Разработка](#разработка)
- [Troubleshooting](#troubleshooting)

## 🛠 Технологический стек

### Backend
- **.NET 8.0** - Основной фреймворк
- **ASP.NET Core Web API** - REST API
- **Entity Framework Core** - ORM для работы с базой данных
- **SQL Server 2022** - База данных
- **QuestPDF** - Генерация PDF отчетов
- **EPPlus** - Работа с Excel файлами

### Frontend
- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик и dev-сервер
- **React Bootstrap** - UI компоненты
- **React Router** - Маршрутизация
- **Axios** - HTTP клиент
- **Google Material Icons** - Иконки

### Инфраструктура
- **Docker** - Контейнеризация
- **Docker Compose** - Оркестрация контейнеров
- **Nginx** - Веб-сервер для frontend

## 📁 Структура проекта

```
Cursa4/
├── backend/                    # Backend приложение
│   ├── CourseProjectAPI/       # Основной проект API
│   │   ├── Controllers/       # API контроллеры
│   │   ├── Services/          # Бизнес-логика
│   │   ├── Models/            # Модели данных
│   │   ├── DTOs/              # Data Transfer Objects
│   │   ├── Data/              # DbContext и миграции
│   │   └── *.sql              # SQL скрипты для БД
│   └── Dockerfile             # Docker образ для backend
├── frontend/                   # Frontend приложение
│   ├── frontend/              # React приложение
│   │   ├── src/
│   │   │   ├── components/    # React компоненты
│   │   │   ├── pages/         # Страницы
│   │   │   ├── services/      # API сервисы
│   │   │   ├── utils/         # Утилиты
│   │   │   └── context/       # React Context
│   │   └── public/            # Статические файлы
│   ├── Dockerfile             # Docker образ для frontend
│   └── nginx.conf             # Конфигурация Nginx
├── docker-compose.yml          # Конфигурация Docker Compose
└── README.md                   # Этот файл
```

## 📦 Предварительные требования

- **Docker Desktop** (версия 20.10 или выше)
- **Docker Compose** (версия 2.0 или выше)
- **Git** (для клонирования репозитория)

Для локальной разработки (без Docker):
- **.NET 8.0 SDK**
- **Node.js** (версия 18 или выше)
- **npm** или **yarn**
- **SQL Server 2022** (или SQL Server Express)

## 🚀 Установка и запуск

### Быстрый старт с Docker (рекомендуется)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd Cursa4
   ```

2. **Запустите проект:**

   **На Windows (рекомендуется):**
   ```cmd
   docker-compose-up.bat
   ```
   
   Или вручную:
   ```powershell
   $env:DOCKER_BUILDKIT=0
   $env:COMPOSE_DOCKER_CLI_BUILD=0
   docker-compose up -d --build
   ```

   **На Linux/macOS:**
   ```bash
   docker-compose up -d --build
   ```

3. **Дождитесь запуска всех сервисов:**
   ```bash
   docker-compose ps
   ```
   
   Все три сервиса должны быть в состоянии "Up":
   - `DBproject` (база данных)
   - `backend-api` (backend)
   - `frontend-app` (frontend)

4. **Проверьте работу:**
   - Frontend: http://localhost:3000
   - Backend API: http://localhost:5171
   - Swagger UI: http://localhost:5171/swagger

### Первоначальная настройка базы данных

После первого запуска необходимо выполнить SQL скрипты для инициализации базы данных:

1. **Подключитесь к контейнеру БД:**
   ```bash
   docker exec -it DBproject /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P Pass1234 -C
   ```

2. **Выполните скрипты инициализации** (в порядке):
   - `Database_Migration_Add_Color_Engine_Tables.sql`
   - `Database_Migration_Add_Configuration_Fields.sql`
   - `Update_Colors_To_LADA_Names.sql`
   - `Fix_Models.sql`
   - `Add_Missing_Models.sql`
   - `Update_Models_And_Configurations.sql`
   - `Add_Additional_Configurations.sql`
   - `Add_Configuration_Descriptions.sql`
   - `Create_Cars_For_All_Models.sql`
   - И другие необходимые скрипты

   **Или используйте готовый скрипт:**
   ```bash
   # Windows
   execute-sql.bat <путь_к_скрипту.sql>
   
   # Linux/macOS
   docker exec -i DBproject /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P Pass1234 -C -i <путь_к_скрипту.sql>
   ```

### Локальная разработка (без Docker)

#### Backend

1. **Перейдите в директорию backend:**
   ```bash
   cd backend/CourseProjectAPI
   ```

2. **Восстановите зависимости:**
   ```bash
   dotnet restore
   ```

3. **Настройте строку подключения** в `appsettings.json`:
   ```json
   {
     "ConnectionStrings": {
       "DefaultConnection": "Server=localhost;Database=Autosalon;User Id=sa;Password=YourPassword;TrustServerCertificate=true;"
     }
   }
   ```

4. **Запустите приложение:**
   ```bash
   dotnet run
   ```

   Backend будет доступен на http://localhost:5171

#### Frontend

1. **Перейдите в директорию frontend:**
   ```bash
   cd frontend/frontend
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Создайте файл `.env`** (если нужно изменить API URL):
   ```
   VITE_API_URL=http://localhost:5171/api
   ```

4. **Запустите dev-сервер:**
   ```bash
   npm run dev
   ```

   Frontend будет доступен на http://localhost:5173

## 💻 Использование

### Основные функции

1. **Каталог автомобилей**
   - Просмотр всех доступных моделей LADA
   - Фильтрация по типу кузова, цене, году выпуска
   - Просмотр детальной информации об автомобиле

2. **Онлайн-конфигуратор**
   - Выбор модели автомобиля
   - Выбор комплектации
   - Выбор двигателя и трансмиссии
   - Выбор цвета
   - Выбор дополнительных опций
   - Просмотр итоговой стоимости

3. **Оформление заказа**
   - Создание заказа на выбранную конфигурацию
   - Управление заказами в личном кабинете
   - Отслеживание статуса заказа

4. **Административная панель** (только для администраторов)
   - Управление автомобилями
   - Управление заказами
   - Импорт автомобилей из Excel
   - Экспорт отчетов по продажам в PDF
   - Просмотр статистики и аналитики

### Роли пользователей

- **Пользователь** - может просматривать каталог, использовать конфигуратор и оформлять заказы
- **Администратор** - имеет доступ ко всем функциям, включая административную панель

### Учетные записи по умолчанию

После инициализации базы данных создайте учетные записи через API или напрямую в базе данных.

## 📚 API документация

После запуска backend, Swagger UI доступен по адресу: http://localhost:5171/swagger

### Основные endpoints

#### Авторизация
- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему

#### Автомобили
- `GET /api/cars` - Получить список автомобилей
- `GET /api/cars/{id}` - Получить информацию об автомобиле
- `GET /api/cars/models` - Получить список моделей
- `GET /api/cars/models/{modelId}/configurations` - Получить комплектации модели
- `GET /api/cars/colors` - Получить список цветов
- `GET /api/cars/engines` - Получить список двигателей
- `GET /api/cars/transmissions` - Получить список трансмиссий
- `PUT /api/cars/{id}` - Обновить информацию об автомобиле (только для админов)

#### Заказы
- `GET /api/orders` - Получить список заказов
- `GET /api/orders/{id}` - Получить информацию о заказе
- `POST /api/orders` - Создать новый заказ
- `PUT /api/orders/{id}/status` - Обновить статус заказа (только для админов)

#### Отчеты
- `GET /api/reports/sales/export` - Экспорт отчета по продажам в PDF

#### Импорт
- `GET /api/cars/import/template` - Скачать шаблон Excel для импорта
- `POST /api/cars/import/excel` - Импортировать автомобили из Excel (только для админов)

## 🗄 База данных

### Структура базы данных

Основные таблицы:
- `Brands` - Бренды автомобилей
- `Models` - Модели автомобилей
- `Configurations` - Комплектации
- `Cars` - Автомобили
- `Colors` - Цвета
- `Engines` - Двигатели
- `Transmissions` - Трансмиссии
- `AdditionalOptions` - Дополнительные опции
- `Orders` - Заказы
- `OrderStatusHistory` - История статусов заказов
- `Users` - Пользователи
- `UserProfiles` - Профили пользователей
- `Roles` - Роли

### Подключение к базе данных

**Через Docker:**
```bash
docker exec -it DBproject /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P Pass1234 -C
```

**Параметры подключения:**
- Сервер: `localhost` (или `db` внутри Docker сети)
- Порт: `1433`
- База данных: `Autosalon`
- Пользователь: `sa`
- Пароль: `Pass1234`

### Выполнение SQL скриптов

**Через Docker:**
```bash
# Windows
execute-sql.bat <путь_к_скрипту.sql>

# Linux/macOS
docker exec -i DBproject /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P Pass1234 -C -i <путь_к_скрипту.sql>
```

## 🔧 Разработка

### Структура компонентов Frontend

```
src/
├── components/
│   ├── common/          # Общие компоненты (Header, Footer, Icon)
│   ├── cars/            # Компоненты для работы с автомобилями
│   │   ├── configurator/ # Компоненты конфигуратора
│   ├── orders/          # Компоненты для работы с заказами
│   └── admin/           # Компоненты административной панели
├── pages/               # Страницы приложения
├── services/            # API сервисы
├── utils/               # Утилиты
└── context/             # React Context (AuthContext)
```

### Добавление новых функций

1. **Backend:**
   - Создайте модель в `Models/`
   - Создайте DTO в `DTOs/`
   - Создайте сервис в `Services/`
   - Создайте контроллер в `Controllers/`
   - Добавьте миграцию БД при необходимости

2. **Frontend:**
   - Создайте компонент в соответствующей директории
   - Добавьте API метод в `services/api/`
   - Добавьте маршрут в `App.jsx` (если нужна новая страница)

### Тестирование

**Backend:**
```bash
cd backend/CourseProjectAPI
dotnet test
```

**Frontend:**
```bash
cd frontend/frontend
npm test
```

## 🐛 Troubleshooting

### Проблемы с Docker

**Контейнер не запускается:**
```bash
# Проверьте логи
docker-compose logs <service-name>

# Пересоберите образы
docker-compose build --no-cache

# Удалите старые контейнеры и volumes
docker-compose down -v
```

**Проблемы с BuildKit на Windows:**
```powershell
$env:DOCKER_BUILDKIT=0
$env:COMPOSE_DOCKER_CLI_BUILD=0
docker-compose up -d --build
```

### Проблемы с базой данных

**База данных не подключается:**
- Проверьте, что контейнер `DBproject` запущен: `docker ps`
- Проверьте логи: `docker logs DBproject`
- Убедитесь, что пароль SA правильный в `docker-compose.yml`

**Ошибки при выполнении SQL скриптов:**
- Убедитесь, что используете правильную кодировку (UTF-8)
- Проверьте, что все зависимости (таблицы, данные) созданы в правильном порядке

### Проблемы с Frontend

**Изображения не загружаются:**
- Проверьте, что файлы находятся в `frontend/frontend/public/images/`
- Проверьте конфигурацию Nginx в `frontend/nginx.conf`

**API запросы не работают:**
- Проверьте переменную окружения `VITE_API_URL`
- Проверьте CORS настройки в backend
- Проверьте, что backend запущен и доступен

### Проблемы с Backend

**Ошибки компиляции:**
```bash
# Очистите кэш
dotnet clean
dotnet restore
dotnet build
```

**Ошибки подключения к БД:**
- Проверьте строку подключения в `appsettings.json`
- Убедитесь, что SQL Server запущен и доступен
- Проверьте firewall настройки

## 📝 Дополнительная информация

### Переменные окружения

**Backend:**
- `ASPNETCORE_ENVIRONMENT` - Окружение (Development/Production)
- `ConnectionStrings__DefaultConnection` - Строка подключения к БД

**Frontend:**
- `VITE_API_URL` - URL backend API

### Порты

- **3000** - Frontend (Nginx)
- **5171** - Backend API
- **1433** - SQL Server

### Безопасность

⚠️ **Важно:** В production окружении:
- Измените пароль SA для SQL Server
- Используйте HTTPS
- Настройте CORS правильно
- Используйте переменные окружения для секретов
- Не коммитьте `appsettings.json` с реальными паролями

