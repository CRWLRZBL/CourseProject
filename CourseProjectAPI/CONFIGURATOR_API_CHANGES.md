# Изменения API для конфигуратора автомобилей

## 📋 Что было добавлено

### 1. Новые поля в таблице `Configurations`

Добавлены следующие поля для хранения характеристик двигателя и коробки передач:

- **EnginePower** (INT, NULL) - Мощность двигателя в л.с.
- **EngineCapacity** (DECIMAL(4,2), NULL) - Объем двигателя (если отличается от базовой модели)
- **FuelType** (NVARCHAR(20), NULL) - Тип топлива (если отличается от базовой модели)
- **TransmissionType** (NVARCHAR(20), NULL) - Тип коробки передач ("Механика", "Автомат", "Вариатор", "Робот")

### 2. Новые API Endpoints

#### Получение цветов
```
GET /api/cars/colors
```
Возвращает список доступных цветов с hex-кодами.

**Ответ:**
```json
[
  {
    "name": "Белый",
    "hexCode": "#FFFFFF"
  },
  {
    "name": "Черный",
    "hexCode": "#000000"
  }
]
```

#### Получение двигателей
```
GET /api/cars/engines?modelId={id}
```
Возвращает список доступных двигателей. Параметр `modelId` опционален.

**Ответ:**
```json
[
  {
    "capacity": 1.6,
    "fuelType": "Бензин",
    "power": 120
  },
  {
    "capacity": 2.0,
    "fuelType": "Дизель",
    "power": 150
  }
]
```

#### Получение коробок передач
```
GET /api/cars/transmissions?modelId={id}
```
Возвращает список доступных коробок передач. Параметр `modelId` опционален.

**Ответ:**
```json
[
  {
    "type": "Механика",
    "description": "Механическая коробка передач"
  },
  {
    "type": "Автомат",
    "description": "Автоматическая коробка передач"
  }
]
```

#### Получение комплектаций (обновлено)
```
GET /api/cars/{carId}/configurations
```
Теперь возвращает комплектации с полной информацией о двигателе и КПП.

**Ответ:**
```json
[
  {
    "configurationId": 1,
    "modelId": 5,
    "configurationName": "Базовая",
    "description": "Базовая комплектация",
    "additionalPrice": 0,
    "enginePower": 120,
    "engineCapacity": 1.6,
    "fuelType": "Бензин",
    "transmissionType": "Механика"
  }
]
```

## 🔧 Что нужно сделать

### 1. Выполнить SQL миграцию

Выполните SQL скрипт `Database_Migration_Add_Configuration_Fields.sql` в вашей базе данных, чтобы добавить новые поля в таблицу `Configurations`.

### 2. Заполнить данные

После выполнения миграции заполните новые поля в таблице `Configurations`:

```sql
-- Пример обновления комплектации
UPDATE Configurations
SET 
    EnginePower = 120,
    EngineCapacity = 1.6,
    FuelType = 'Бензин',
    TransmissionType = 'Механика'
WHERE ConfigurationID = 1;
```

### 3. Обновить фронтенд

На фронтенде теперь можно использовать реальные данные из API вместо демо-данных:

```typescript
// Вместо DEFAULT_COLORS
const colors = await carService.getAvailableColors();

// Вместо DEFAULT_ENGINES
const engines = await carService.getAvailableEngines(modelId);

// Вместо DEFAULT_TRANSMISSIONS
const transmissions = await carService.getAvailableTransmissions(modelId);
```

## 📝 Примечания

1. **Мощность двигателя**: Если в комплектации не указана мощность (`EnginePower`), она будет `null` в ответе API.

2. **Объем и тип топлива**: Если в комплектации не указаны `EngineCapacity` и `FuelType`, будут использоваться значения из базовой модели (`Models`).

3. **Коробка передач**: Если в БД нет данных о КПП в комплектациях, API вернет стандартный список (Механика, Автомат, Вариатор, Робот).

4. **Обратная совместимость**: Все новые поля опциональны (NULL), поэтому существующие данные не будут затронуты.

## 🚀 Готово к использованию

После выполнения миграции и заполнения данных, все endpoints готовы к использованию!

